#!/bin/sh

# Dmenu to choose window or select shot

choices="Cancel\nWindow\nSelect"
prompt="Please choose:"

choice=$(echo -e "$choices" | ddmenu -sb "#ad1616" -p "choose")

case "$choice" in 
    Cancel) exit ;z\;
    Window) shot -u && exit & ;;
    Select) shot -s && exit & ;;
esac
