#!/bin/sh

# Dmenu to choose window or select shot

choices="Window\nSelect"
prompt="Please choose:"

choice=$(echo "$choices" | dmenu -p "choose")

case "$choice" in 
    Window) shot -u && exit & ;;
    Select) shot -s && exit & ;;
esac
